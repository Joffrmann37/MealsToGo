import { colors } from "./colors";
import { spacing } from "./spacing";
import { sizes } from "./sizes";
import { fonts } from "./fonts";

export const theme = {
  colors,
  spacing,
  sizes,
  fonts,
};
